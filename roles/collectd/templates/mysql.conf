<Plugin "mysql">
{% for db in databases %}
  <Database "{{ db.name }}">
    Host "{{ db.hostname }}"
    User "{{ db.username }}"
    Password "{{ db.password }}"
    Port 3306
    MasterStats true
  </Database>
{% endfor %}
</Plugin>
