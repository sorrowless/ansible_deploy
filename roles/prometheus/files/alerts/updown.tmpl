ALERT InstanceReeboted
  IF graphite_events_uptime < 200
  ANNOTATIONS {
    summary = "Instance {{ $labels.host }} has been rebooted",
    description = "{{ $labels.host }} of job {{ $labels.job }} has been rebooted.",
  }
